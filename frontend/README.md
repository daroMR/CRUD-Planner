# Frontend Dashboard (Pruebas)

Puedes usar cualquier framework JS, pero para pruebas rápidas puedes usar un HTML simple con fetch o una app React/Vite.

Recomendación: crea un dashboard que consuma los endpoints de FastAPI y muestre los logs de cada operación (GET, POST, PUT, DELETE) en pantalla.

Ejemplo de estructura:
- Formulario para crear plan, bucket, tarea
- Listado de planes, buckets, tareas
- Panel/log de resultados de cada operación
